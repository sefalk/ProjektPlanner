# Python
.venv/
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
backend/.coverage

# Environment / config
.env
.env.*
!.env.example

# Database — NEVER commit (contains sensitive/DSGVO data)
*.db
*.sqlite
*.sqlite3
*.db.bak
*.sqlite.bak

# Local runtime config (may contain absolute paths / usernames)
backend/data_config.json

# Real seed / production data — NEVER commit
seed/real/
data/

# Review / planning documents with real data — keep local only
/*.txt

# Frontend
node_modules/
frontend/dist/
frontend/.vite/
frontend/playwright-report/
frontend/test-results/
*.local

# IDE / OS
.vscode/
.idea/
.claude/
*.swp
.DS_Store
Thumbs.db

# Alembic (generated files ok, but not local override)
alembic/versions/*.pyc
